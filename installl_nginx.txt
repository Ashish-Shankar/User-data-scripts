#!/bin/bash

# Update package list
sudo apt update -y

# Install Nginx
sudo apt install nginx -y

# Start Nginx service
sudo systemctl start nginx

# Enable Nginx at boot
sudo systemctl enable nginx

# Create a simple test page
echo "<h1>Nginx installed successfully from User Data</h1>" | sudo tee /var/www/html/index.html